enum Rainbow { Merah, Jingga, Kuning, Hijau, Biru, Nila, Ungu }

enum TaskStatus { Todo, InProgress, InReview, Done }

void main() {
  print(Rainbow.values);
  print(Rainbow.Kuning);
  print(Rainbow.Biru.index);
}
