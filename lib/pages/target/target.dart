import 'package:flutter/material.dart';

class target extends StatelessWidget {
  const target({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('TARGET MENABUNG'),
    ));
  }
}
